.name "TEST_ADD"
.comment "test add instruction!!!!!"

# add lhs, rhs, dst

# Computes lhs + rhs and stores the result in register dst
# Result affects zf

# x cycles

# { T_REG, T_REG, T_REG }

add r2, r4, r5
add r1, r1, r1
add r10, r15, r16
add r8, r12, r3
